{
  "name": "dedupe",
  "displayName": "dedupe",
  "description": "dedupe list of things",
  "version": "0.0.1",
  "publisher": "test",
  "engines": {
    "vscode": "^1.11.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onCommand:extension.dedupe"],
  "main": "./extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.dedupe",
        "title": "dedupe"
      }
    ]
  },
  "scripts": {
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "vscode": "^1.0.0"
  }
}
